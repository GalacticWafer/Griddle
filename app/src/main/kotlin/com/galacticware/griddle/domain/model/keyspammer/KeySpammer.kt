package com.galacticware.griddle.domain.model.keyspammer

/**
 * We only spam backspace right now but we could spam space and allow custom spam implementation for
 * other kinds of spamming, such as regular spacebar spamming (which does not exist right now).
 */
abstract class KeySpammer