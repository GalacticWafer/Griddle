package com.galacticware.griddle.domain.model.usercontolled

/**
 * Enumeration to specify whether Vibration should be on or off in a type-safe way.
 */
enum class VibrationChoice {
    ON,
    OFF,
}
/**
 * Enumeration to specify whether Vibration should be on or off in a type-safe way.
 */
enum class TurboModeChoice {
    ON,
    OFF,
}

enum class GestureTracingChoice {
    ON,
    OFF,
}